{
  "buildCommand": "npm run build",
  "installCommand": "npm install --production=false",
  "outputDirectory": "dist",
  "public": "dist",
  "routes": [
    { "src": "/api/(.*)", "dest": "/api" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
